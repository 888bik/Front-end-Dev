<!-- components/area-header/area-header.wxml -->
<view class="area-header">
  <view class="title">{{title}}</view>
  <view class="more" wx:if="{{hasMore}}">
    <text class="text" bind:tap="onRecommendMoreTap">更多</text>
    <van-icon name="arrow" />
  </view>
</view>